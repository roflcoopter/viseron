An object detector scans an image to identify multiple objects and their position.

:::tip

Object detectors can be taxing on the system, so it is wise to combine it with a [motion detector](/docs/introduction/configuration#motion-detector-domain)

:::
