trigger: none

resources:
  pipelines:
    - pipeline: Viseron Pipeline
      source: Viseron
      trigger:
        branches:
          include:
            - dev
            - docker_revamp
jobs:
  - template: templates/build.yaml
    parameters:
      image: base
