trigger: none

pool:
  vmImage: "ubuntu-latest"

resources:
  pipelines:
    - pipeline: viseronModels
      source: "Viseron Models"
      trigger:
        enabled: true

jobs:
  - template: templates/build.yaml
    parameters:
      image: base
