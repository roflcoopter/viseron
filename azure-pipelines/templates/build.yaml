parameters:
  - name: architectures
    type: object
    default:
      - amd64
      - rpi3
  - name: image
    type: string

jobs:
  - job: "build_${{ parameters.image }}"
    strategy:
      matrix:
        ${{ each architecture in parameters.architectures }}:
          architecture: ${{ architecture }}
    pool:
      vmImage: "ubuntu-latest"
    steps:
      - script: docker run --rm --privileged multiarch/qemu-user-static:register --reset
        displayName: Register QEMU for cross-builds
      - script: |
          cd $(Agent.BuildDirectory)/s/docker
          echo Building ${{ parameters.image }} for $(architecture)
          docker-compose --file ../azure-pipelines/docker-compose-build.yaml --env-file ../azure-pipelines/.env build $(architecture)-${{ parameters.image }}
          done
        displayName: Build ${{ parameters.image }}
